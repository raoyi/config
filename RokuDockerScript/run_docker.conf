# Docker 挂载路径配置
HOST_PATH="$HOME/device/"
CONTAINER_PATH="/root/automated_tests/device"

# 镜像更新配置（全部填写才会触发检查，任一为空则跳过）
REMOTE_HOST="labb-ot910215@192.168.31.152"
REMOTE_PASS="ot910215"
REMOTE_CONFIG="/home/labb-ot910215/Desktop/roku/DockerImageMaster/DockerInUse"
REMOTE_IMAGE_DIR="/home/labb-ot910215/Desktop/roku/DockerImageMaster"
IMAGE_FILTER="framework-external"

# 可选配置
# DOWNLOAD_DIR="/tmp/docker_images"   # 临时下载目录，默认 /tmp/docker_images
# KEEP_TAR="false"                    # 加载后保留 tar 文件，默认 false
# SSH_OPTS="-q -o StrictHostKeyChecking=no -o ConnectTimeout=10"
